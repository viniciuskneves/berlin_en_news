name: Main

on:
  push:
    branches: [test]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Typescript build
        run: npm run typescript:build
      - name: Test
        uses: ./.github/actions/aws-sam/
        with:
          command: sam build
